# Версия 44

Исправление критической ошибки загрузки данных.

- Устранена ошибка `themeVars is not defined`.
- В новых функциях иерархии рек заменён неверный вызов `themeVars()` на существующую функцию `styleVars()`.
- Обновлены ссылки кеширования до `style.css?v=44` и `app.js?v=44`.
- APP_VERSION обновлён до `44`.
