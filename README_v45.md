# Версия 45

Hotfix:

- исправлена причина, из-за которой режим экспорта не открывался: рекурсивный вызов `ensureExportFlags()` ↔ `syncExportContextText()`;
- добавлена отдельная плавающая кнопка «Экспорт карты» справа по центру экрана с подсказкой при наведении;
- старая кнопка в левой панели сохранена;
- обновлены версии `app.js?v=45`, `style.css?v=45`, `APP_VERSION = 45`.
